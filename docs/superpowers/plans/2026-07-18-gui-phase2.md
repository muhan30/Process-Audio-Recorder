# 阶段二 GUI 包装·实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 把阶段一调试好的命令行引擎包一层 Win32 原生图形界面，变成可双击运行、系统托盘常驻的自用录音工具。

**Architecture:** 引擎层零改动——GUI 层通过直接调用引擎模块的公共接口获取数据并下发指令。技术栈为 Win32 API（CreateWindow/Common Controls/Dialog/Shell_NotifyIcon），语言沿用项目现有的 C++17，CMake 构建。单 exe 产物（链接所有引擎 .o，无第三方 GUI 依赖）。

**Tech Stack:** C++17 / Win32 GDI + Common Controls（静态链接）/ WIL（资源管理）/ WASAPI + MF（引擎层，不改）。

**通用约定：**
- 构建命令 `cmake --build build`，零 error 零 warning（clang -Winvalid-offsetof 既有的 3 个 warning 除外）。
- 每个任务完成即 commit；提交信息英文。
- 所有新文件 UTF-8 编码。
- 👤 用户验证步骤是每个任务的验收关卡，必须等用户确认。

---

### Task 1: 引擎接口提取 —— 梳理并封装 GUI 可调用的一切

**目标**：为了避免松散耦合，重构 CLoopbackCapture 的部分方法，将 AudioSessionLister、MicCapture、AudioMixer 和 CLoopbackCapture 的数据获取接口封装为简洁的 GUI 回调式接口。引入 CaptureEngine 类聚拢所有操作，GUI 只与这一个类交互。

**Files:**
- Modify: `source/LoopbackCapture.h/.cpp`：导出进度和电平回调，接收外部停止指令
- Modify: `source/MicCapture.h/.cpp`：暴露音频格式数据获取接口
- Create: `source/CaptureEngine.h/.cpp`：统一 GUI 入口，聚合 Loopback+M4A+Mixer 逻辑

- [ ] **Step 1.1: 重写 `source/CaptureEngine.h`（GUI 唯一对话对象）**

```cpp
/*
 * GUI 与引擎的唯一接口：封装环回捕获、麦克风采集、混音和电平反馈。
 * 所有复杂状态管理在此完成，表现层（GUI）仅需要调用此类的公开方法。
 */
#pragma once

#include "AudioSink.h"
#include "AudioMixer.h"
#include "LevelMeter.h"
#include <functional>
#include <memory>
#include <string>
#include <atomic>

class MicCapture;
class CLoopbackCapture;

// 供 GUI 消费的录音状态快照
struct CaptureStatus
{
    std::chrono::seconds elapsed{ 0 };
    UINT64 bytesWritten = 0;
    int systemLevel = 0;
    int micLevel = 0;
    bool micEnabled = false;
};

class CaptureEngine
{
public:
    using StatusCallback = std::function<void(const CaptureStatus&)>;
    using StoppedCallback = std::function<void(HRESULT)>;

    CaptureEngine();
    ~CaptureEngine();

    // 设置输出参数
    void SetOutputPath(const std::wstring& path);
    void SetOutputFormat(const std::wstring& fmt); // "m4a" or "wav"
    void SetMicEnabled(bool enabled);
    void SetMicGain(float gain);
    void SetSystemGain(float gain);

    // 捕获控制（Start 异步，立即返回；Stop 阻塞等待完成）
    HRESULT Start(DWORD processId, bool includeProcessTree,
                  StatusCallback onStatus, StoppedCallback onStopped);
    HRESULT StartGlobal(StatusCallback onStatus, StoppedCallback onStopped);
    void Stop();

private:
    struct Impl;
    std::unique_ptr<Impl> m_impl;
};
```

- [ ] **Step 1.2: 重写 `source/CaptureEngine.cpp`（引擎内部桥接）**

```cpp
#include "CaptureEngine.h"

#include "LoopbackCapture.h"
#include "MicCapture.h"
#include "WavSink.h"
#include "M4aSink.h"
#include "AudioMixer.h"
#include "LevelMeter.h"

#include <wil/resource.h>
#include <chrono>
#include <memory>

struct CaptureEngine::Impl
{
    CLoopbackCapture loopback;
    MicCapture mic;
    AudioMixer mixer;
    LevelState levelState;

    std::unique_ptr<AudioSink> sink;
    std::wstring outputPath;
    std::wstring outputFormat{ L"m4a" };
    bool micEnabled = false;

    StatusCallback onStatus;
    StoppedCallback onStopped;
    std::atomic<bool> isCapturing{ false };
    std::thread statusThread;
};

CaptureEngine::CaptureEngine() : m_impl(std::make_unique<Impl>()) {}

CaptureEngine::~CaptureEngine()
{
    if (m_impl->isCapturing)
        Stop();
}

void CaptureEngine::SetOutputPath(const std::wstring& path)   { m_impl->outputPath = path; }
void CaptureEngine::SetOutputFormat(const std::wstring& fmt)   { m_impl->outputFormat = fmt; }
void CaptureEngine::SetMicEnabled(bool enabled)                { m_impl->micEnabled = enabled; }
void CaptureEngine::SetMicGain(float gain)                     { m_impl->mixer.SetMicGain(gain); }
void CaptureEngine::SetSystemGain(float gain)                  { m_impl->mixer.SetSystemGain(gain); }

HRESULT CaptureEngine::Start(DWORD pid, bool includeTree,
                              StatusCallback onStatus, StoppedCallback onStopped)
{
    auto& impl = *m_impl;
    impl.onStatus = std::move(onStatus);
    impl.onStopped = std::move(onStopped);

    // 创建 Sink
    if (impl.outputFormat == L"wav")
        impl.sink = std::make_unique<WavSink>();
    else
        impl.sink = std::make_unique<M4aSink>();
    impl.loopback.SetAudioSink(impl.sink.get());

    // 统一分流（电平 + 混音/直通）
    impl.mixer.SetSystemGain(impl.mixer.GetSystemGain()); // 继承外部设置的增益
    impl.loopback.SetDataTap([&impl](std::vector<BYTE>&& chunk) {
        impl.levelState.systemLevel = CalcPeakLevel(chunk.data(), static_cast<DWORD>(chunk.size()));
        if (impl.micEnabled)
            impl.loopback.EnqueueAudioData(impl.mixer.MixWithLoopback(std::move(chunk)));
        else
            impl.loopback.EnqueueAudioData(impl.mixer.ApplySystemGain(std::move(chunk)));
    });

    if (impl.micEnabled)
    {
        RETURN_IF_FAILED(impl.mic.Initialize([&impl](const BYTE* data, DWORD size) {
            impl.levelState.micLevel = CalcPeakLevel(data, size);
            impl.mixer.PushMicData(data, size);
        }));
    }

    // 启动捕获
    RETURN_IF_FAILED(impl.loopback.StartCaptureAsync(pid, includeTree, impl.outputPath.c_str()));
    if (impl.micEnabled)
        RETURN_IF_FAILED(impl.mic.Start());

    impl.isCapturing = true;

    // 状态上报线程（ 200ms 一次）
    impl.statusThread = std::thread([&impl, start = std::chrono::steady_clock::now()]() mutable {
        while (impl.isCapturing) {
            std::this_thread::sleep_for(std::chrono::milliseconds(200));
            if (!impl.isCapturing) break;
            CaptureStatus st;
            st.elapsed = std::chrono::duration_cast<std::chrono::seconds>(
                std::chrono::steady_clock::now() - start);
            st.bytesWritten = impl.sink ? impl.sink->BytesWritten() : 0;
            st.systemLevel = impl.levelState.systemLevel;
            st.micLevel = impl.levelState.micLevel;
            st.micEnabled = impl.micEnabled;
            if (impl.onStatus) impl.onStatus(st);
        }
    });
    return S_OK;
}

HRESULT CaptureEngine::StartGlobal(StatusCallback onStatus, StoppedCallback onStopped)
{
    auto& impl = *m_impl;
    impl.onStatus = std::move(onStatus);
    impl.onStopped = std::move(onStopped);

    if (impl.outputFormat == L"wav")
        impl.sink = std::make_unique<WavSink>();
    else
        impl.sink = std::make_unique<M4aSink>();
    impl.loopback.SetAudioSink(impl.sink.get());
    impl.loopback.SetDataTap([&impl](std::vector<BYTE>&& chunk) {
        impl.levelState.systemLevel = CalcPeakLevel(chunk.data(), static_cast<DWORD>(chunk.size()));
        // 全局模式不混麦克风（待未来扩展）
        impl.loopback.EnqueueAudioData(impl.mixer.ApplySystemGain(std::move(chunk)));
    });

    RETURN_IF_FAILED(impl.loopback.StartGlobalCaptureAsync(impl.outputPath.c_str()));
    impl.isCapturing = true;

    impl.statusThread = std::thread([&impl, start = std::chrono::steady_clock::now()]() mutable {
        while (impl.isCapturing) {
            std::this_thread::sleep_for(std::chrono::milliseconds(200));
            if (!impl.isCapturing) break;
            CaptureStatus st;
            st.elapsed = std::chrono::duration_cast<std::chrono::seconds>(
                std::chrono::steady_clock::now() - start);
            st.bytesWritten = impl.sink ? impl.sink->BytesWritten() : 0;
            st.systemLevel = impl.levelState.systemLevel;
            st.micLevel = 0;
            st.micEnabled = false;
            if (impl.onStatus) impl.onStatus(st);
        }
    });
    return S_OK;
}

void CaptureEngine::Stop()
{
    auto& impl = *m_impl;
    if (!impl.isCapturing) return;

    if (impl.micEnabled)
        impl.mic.Stop();
    HRESULT hr = impl.loopback.StopCaptureAsync();
    impl.isCapturing = false;
    if (impl.statusThread.joinable())
        impl.statusThread.join();
    if (impl.onStopped)
        impl.onStopped(hr);
}
```

- [ ] **Step 1.3: 修改 `source/AudioMixer.h`（暴露增益读值）**

```cpp
public:
    float GetSystemGain() const { return m_systemGain.load(); }
    float GetMicGain() const { return m_micGain.load(); }
```

- [ ] **Step 1.4: 修改 `CMakeLists.txt`**

```cmake
add_executable(ProcessAudioRecorderGUI
    source/GUI/Main.cpp
    source/GUI/MainWindow.cpp
    source/GUI/SettingsDialog.cpp
    source/GUI/TrayIcon.cpp
    source/CaptureEngine.cpp
    source/LoopbackCapture.cpp
    source/AudioSessionLister.cpp
    source/WavSink.cpp
    source/M4aSink.cpp
    source/MicCapture.cpp
    source/AudioMixer.cpp
)
```

（GUI 独立为 `ProcessAudioRecorderGUI`，现有 CLI 产物不受影响。）

- [ ] **Step 1.5: 编译验证**

Run: `cmake --build build`
Expected: CaptureEngine 编译通过（MainWindow 尚不存在会链接失败，单独检查 .o 即可）。

- [ ] **Step 1.6: 👤 验证**

重跑 `--list` / `--mic on --sys-gain 2 --mic-gain 4` 确认引擎路径未被改动影响。

- [ ] **Step 1.7: Commit**

```bash
git add source/CaptureEngine.h source/CaptureEngine.cpp source/AudioMixer.h CMakeLists.txt
git commit -m "Add CaptureEngine facade for GUI integration"
```

---

### Task 2: 主窗口骨架 —— Win32 窗口创建与控件布局

**目标**：构建待机与录音两个状态页，连接 CaptureEngine 回调，实现列表刷新、开始/停止整个闭环。

**Files:**
- Create: `source/GUI/Main.cpp`（WinMain 入口 + 消息泵）
- Create: `source/GUI/MainWindow.h/.cpp`（主窗口创建、控件事件、状态切换）
- Create: `source/GUI/SessionList.cpp`（用 AudioSessionLister 填充 ListView）

- [ ] **Step 2.1: 写 `source/GUI/Main.cpp`**

- 注册窗口类，创建主窗口
- 注册托盘图标类
- 运行标准消息循环
- 确保进程单实例（CreateMutex）

- [ ] **Step 2.2: 写 `source/GUI/MainWindow.h/.cpp`**

- 创建子控件：按钮（开始/停止）、ListView（会话）、CheckBox（麦克风）、Static（计时/电平）
- 双击列表行 → 选中会话；点击开始 → 切换布局为录音态
- 定时器（WM_TIMER 200ms）刷新进度和电平（从 CaptureEngine 取状态）

- [ ] **Step 2.3: 写 `source/GUI/SessionList.h/.cpp`**

- 封装 ListView，提供 Refresh/GetSelectedPid 方法
- 内部调用 AudioSessionLister（COM 已全局初始化）

- [ ] **Step 2.4: 👤 验证（仅窗口骨架，尚未接真实录音）**

1. 双击 GUI exe → 窗口出现，列表显示正在发声软件
2. 点列表选择一行，点开始 → 布局切换为录音态（计时数字走、电平条默认 0）
3. 点停止 → 切回待机态

---

### Task 3: 录音闭环 —— 启动/停止/电平显示 (基于 Task 2 的骨架)

**目标**：完整的录音生命周期：开始 → 混音 → 电平 → 停止 → 保存。

**涉及文件:** MainWindow.cpp（修改Timer回调、开始停止按钮逻辑）

- [ ] **Step 3.1: 在 MainWindow 中实例化 CaptureEngine**

```cpp
CaptureEngine m_engine;                          // 引擎实例
std::chrono::steady_clock::time_point m_recStart; // 录音起始时间
```

- [ ] **Step 3.2: 开始按钮 → StartCapture**

- 从 SessionList 取所选 PID
- 获取 micEnabled/micGain/sysGain 从控件值或默认值
- 调用 `m_engine.Start(pid, …, m_engine::onStatus, m_engine::onStopped)`
- 成功后切视图

- [ ] **Step 3.3: 停止按钮 → m_engine.Stop()**

- 调用 Stop 并等待 onStopped 回调
- 切回待机，显示保存提示

- [ ] **Step 3.4: 👤 验证（第一次闭环）**

1. 放歌 → 打开软件 → 列表选酷狗 → 开始
2. SYS 电平条随歌跳 → 说话时 MIC 条跳
3. 停止 → 文件生成，播放验证

---

### Task 4: 设置对话框

**目标**：文件路径选择、格式切换、麦克风设备选择。

**Files:**
- Create: `source/GUI/SettingsDialog.h/.cpp`

- [ ] **Step 4.1: 创建设置对话框（DialogBox 或 无模式弹出窗口）**

- 路径：Edit + Browse 按钮（SHBrowseForFolder）
- 格式：Radio buttons（M4A/WAV）
- 麦克风设备：ComboBox 填充设备列表（IMMDeviceEnumerator）
- 增益：两个 TrackBar（范围 0.1-8.0，初始值 sys=2 mic=4）
- OK/Cancel 按钮；OK 后写入 .ini

- [ ] **Step 4.2: 配置持久化**

- 用 GetPrivateProfileString/WritePrivateProfileString 写入 `ProcessAudioRecorder.ini`
- 启动时读 ini 恢复上次设置

- [ ] **Step 4.3: 👤 验证**

1. 打开设置 → 修改保存路径 → 关闭窗口
2. 再次打开设置 → 路径仍为修改后的值；检查程序目录生成 .ini

---

### Task 5: 托盘集成

**目标**：缩到托盘、右键菜单、红点提示。

**Files:**
- Create: `source/GUI/TrayIcon.h/.cpp`

- [ ] **Step 5.1: 创建托盘图标并处理事件**

- Shell_NotifyIcon + 图标资源（先用系统默认图标，后续替换）
- 窗口关闭消息拦截为 ShowWindow(SW_HIDE) 而非退出
- 右键菜单（CreatePopupMenu + TrackPopupMenu）

- [ ] **Step 5.2: 录音时红点**

- LoadIcon 或 CreateSolidBrush 生成红点覆盖图标
- 录音中 m_isRecording 标志 → 切换图标

- [ ] **Step 5.3: 👤 验证**

1. 打开软件 → 开始录音 → 点 ✕ → 窗口消失、托盘出图标红点
2. 双击托盘 → 窗口恢复，计时仍在跑
3. 停止 → 红点消失

---

### Task 6: 打包整理

**目标**：添加 GUI 专用图标（.ico）、编译 .rc 加入版本信息、解决 CMake 对 UTF-8 源码的正确编译选项。

**Files:**
- Create: `res/app.ico`
- Modify: `CMakeLists.txt`（嵌入资源）
- Modify: `source/ProcessAudioRecorder.rc`（或者新建 .rc 给 GUI）

- [ ] **Step 6.1: 用系统图标作为临时方案 + 后期换正式图标**

- 从 Visual Studio 图标库复制通用音频图标
- 替换托盘和主窗口图标

- [ ] **Step 6.2: CMake 资源嵌入**

```cmake
target_sources(ProcessAudioRecorderGUI PRIVATE source/GUI/resource.rc)
```

- [ ] **Step 6.3: 👤 最终验收**

将所有引擎 + GUI 编译产物替换 .exe → 双击 → 列表 → 录音 → 关闭 → 托盘恢复 → 停止 → 播放 → 全部正常。

---

## 阶段二完成验收清单

- [ ] 列表显示正确（🔊/安静）
- [ ] 录音 → 电平条 → 停止 → 文件可听
- [ ] 混音 + 增益滑块生效（默认 sys=2, mic=4）
- [ ] 托盘 ✕ 缩窗 + 红点 + 恢复
- [ ] 退出确认（录音中退出弹提示框）
- [ ] 设置持久化（.ini 重启保留上次选择）
- [ ] 防空录（安静进程电平条一动不动）
- [ ] 单 exe，双击即用
